import { useLocation } from "react-router-dom";

export default function MySpeeds() {
  // fix it
  // const location = useLocation();
  // const { measurementSystem } = location.state;
  return (
    <>
      <h1>MySpeeds Placeholder</h1>
    </>
  );
}
